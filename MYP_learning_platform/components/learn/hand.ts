import { Caveat } from 'next/font/google'

// Handwriting face for the teacher's margin notes, scoped to the DP Physics lessons only.
export const hand = Caveat({ subsets: ['latin'], weight: ['500', '700'], display: 'swap' })
